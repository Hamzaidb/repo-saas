# Root .env.example
# Copy relevant sections into apps/backend/.env and apps/frontend/.env
# or export them in your shell / CI.

###############################
# Backend (Fastify + Prisma)  #
###############################
# Service
NODE_ENV=development
PORT=3001
APP_URL=http://localhost:3000

# Database (Prisma)
# e.g. postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public
DATABASE_URL=

# Auth / JWT
JWT_SECRET=replace-with-a-long-random-secret

# CORS (optional; comma-separated origins or leave empty to use defaults in plugin)
CORS_ORIGIN=

# Stripe (server)
STRIPE_SECRET_KEY=sk_test_xxx
# Webhook secret from Stripe CLI or Dashboard
STRIPE_WEBHOOK_SECRET=whsec_xxx

###############################
# Frontend (Next.js)          #
###############################
# Public API base URL the frontend will call for the backend
NEXT_PUBLIC_API_URL=http://localhost:3001

# Supabase (browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Stripe (browser)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# Optional App URL (used in redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000
